import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {

    },
    input: {
        borderWidth: 1,
        borderColor: '#bdbdbd',
        borderRadius: 5,
        padding: 10,
        margin: 10,
    }
});